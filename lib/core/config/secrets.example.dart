// Copy this file to secrets.dart and fill in real values.
// secrets.dart is git-ignored — NEVER commit the real file.
class AppSecrets {
  /// Gmail address used for sending OTP and feedback emails.
  static const smtpEmail = 'your-app-email@gmail.com';

  /// Gmail App Password (not your Google account password).
  /// Generate at: https://myaccount.google.com/apppasswords
  static const smtpPassword = 'xxxx-xxxx-xxxx-xxxx';
}
