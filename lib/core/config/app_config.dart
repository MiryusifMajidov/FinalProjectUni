/// App configuration constants.
/// Sensitive API keys have been moved to server-side (Cloud Functions + Secret Manager).
class AppConfig {
  // EmailJS credentials removed — email sending is now handled by Cloud Functions.
}
